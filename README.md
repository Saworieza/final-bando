
## Running App

```
    composer install
    cp .env.example .env
    php artisan key:generate
    php artisan migrate --seed
    php artisan serve

    rm -rf node_modules/.vite
    npm install
    npm run dev

```

