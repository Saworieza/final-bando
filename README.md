
## Running App

```
    composer install
    cp .env.example .env
    php artisan key:generate
    sudo apt install php-sqlite3 -- install sqlite
    php artisan migrate --seed
    php artisan storage:link
    php artisan serve

    rm -rf node_modules/.vite
    npm install
    npm run dev

```

